# ANSIBLE AUTOMATION

We had to manually run shell script jobs in every machine for a service to restart. We used ansible to automate this task.
We created a task to create a list of the machines using tags in EC2 and then ran tasks to run the script and check if jobs are running after restarting the jobs

This was a project I led with help of interns 
